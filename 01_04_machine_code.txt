# Exercise 4: Which of the following contains “machine code”?
# a) The Python interpreter
# b) The keyboard
# c) Python source file
# d) A word processing document
# b) is an input device and d) does not contain machine code at all.
# c) python source file is written in high-level language, but can be converted to machine code.
# a) the python interpreter is compiled into machine code from a high-level programming language C, so the answer is a)
